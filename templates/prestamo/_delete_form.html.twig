<form method="post" action="{{ path('app_prestamo_delete', {'id': prestamo.id}) }}" onsubmit="return confirm('¿Estas seguro que quires borrar este prestamo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestamo.id) }}">
    <button class="btn">Borrar</button>
</form>
